# Development & CI dependencies. Installs runtime deps too.
-r requirements.txt

# Testing
pytest==8.3.4
pytest-cov==6.0.0
respx==0.21.1          # httpx request mocking for the forecasting tool

# Linting / formatting / typing
ruff==0.8.4
black==24.10.0
mypy==1.14.0
types-PyYAML==6.0.12.20240917

# Pre-commit hook runner
pre-commit==4.0.1
